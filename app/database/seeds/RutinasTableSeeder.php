<?php

class RutinasTableSeeder extends Seeder {

    public function run()
    {
    	// Uncomment the below to wipe the table clean before populating
    	// DB::table('rutinas')->delete();

        $rutinas = array(

        );

        // Uncomment the below to run the seeder
        // DB::table('rutinas')->insert($rutinas);
    }

}