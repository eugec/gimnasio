<?php

class MaquinasTableSeeder extends Seeder {

    public function run()
    {
    	// Uncomment the below to wipe the table clean before populating
    	// DB::table('maquinas')->delete();

        $maquinas = array(

        );

        // Uncomment the below to run the seeder
        // DB::table('maquinas')->insert($maquinas);
    }

}