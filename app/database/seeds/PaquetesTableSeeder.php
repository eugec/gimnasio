<?php

class PaquetesTableSeeder extends Seeder {

    public function run()
    {
    	// Uncomment the below to wipe the table clean before populating
    	// DB::table('paquetes')->delete();

        $paquetes = array(

        );

        // Uncomment the below to run the seeder
        // DB::table('paquetes')->insert($paquetes);
    }

}